enum Color {
    Red,
    Green,
    Blue,
}

fn main() {
    let color = Red;
    let result = match color {
        Red => 1,
        Green => 2,
        Blue => 3,
    };
    println!("Color value: {}", result);
}