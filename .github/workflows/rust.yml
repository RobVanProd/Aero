name: Rust CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  CARGO_TERM_COLOR: always
  AERO_LLVM_OPT: /usr/bin/opt-22
  AERO_LLVM_AS: /usr/bin/llvm-as-22
  AERO_REQUIRE_LLVM_VERIFIER: "true"

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        rust: [stable, nightly]

    steps:
    - uses: actions/checkout@v4
    - name: Install Rust ${{ matrix.rust }}
      uses: dtolnay/rust-toolchain@master
      with:
        toolchain: ${{ matrix.rust }}
        components: clippy, rustfmt

    - name: Build
      run: cargo build --verbose
      working-directory: src/compiler

    - name: Install pinned LLVM 22 + Clang 22
      run: |
        sudo install -m 0755 -d /etc/apt/keyrings
        curl -fsSL https://apt.llvm.org/llvm-snapshot.gpg.key \
          | sudo gpg --dearmor --yes -o /etc/apt/keyrings/apt.llvm.org.gpg
        . /etc/os-release
        echo "deb [signed-by=/etc/apt/keyrings/apt.llvm.org.gpg] http://apt.llvm.org/${VERSION_CODENAME}/ llvm-toolchain-${VERSION_CODENAME}-22 main" \
          | sudo tee /etc/apt/sources.list.d/llvm-22.list
        sudo apt-get update
        sudo apt-get install -y --no-install-recommends llvm-22 clang-22
        opt-22 --version
        llvm-as-22 --version
        llc-22 --version
        clang-22 --version

    - name: Verify documented Quick Start
      if: matrix.rust == 'stable'
      shell: bash
      run: |
        export PATH="/usr/lib/llvm-22/bin:${PATH}"
        test "$(command -v clang)" = "/usr/lib/llvm-22/bin/clang"
        test "$(command -v llc)" = "/usr/lib/llvm-22/bin/llc"
        clang --version | grep -Eq 'clang version 22\.'
        llc --version | grep -Eq 'LLVM version 22\.'
        test "${AERO_LLVM_OPT}" = "/usr/bin/opt-22"
        test "${AERO_LLVM_AS}" = "/usr/bin/llvm-as-22"
        cargo build --release --manifest-path src/compiler/Cargo.toml
        export PATH="${GITHUB_WORKSPACE}/src/compiler/target/release:${PATH}"
        aero --version
        quick_start_root="$(mktemp -d)"
        cd "${quick_start_root}"
        aero init my_app
        cd my_app
        aero check src/main.aero
        set +e
        run_output="$(aero run src/main.aero)"
        run_status=$?
        set -e
        printf '%s\n' "${run_output}"
        test "${run_status}" -eq 0
        output_count="$(printf '%s\n' "${run_output}" | grep -c '^Output: Hello, Aero!$' || true)"
        test "${output_count}" -eq 1

    - name: Confirm LLVM 22 rejects known-invalid IR
      if: matrix.rust == 'stable'
      shell: bash
      run: |
        invalid_ir="${RUNNER_TEMP}/aero-known-invalid.ll"
        printf '%s\n' \
          'define i32 @known_invalid() {' \
          'entry:' \
          '  ret i64 0' \
          '}' > "${invalid_ir}"
        if opt-22 -passes=verify -disable-output "${invalid_ir}"; then
          echo "opt-22 accepted known-invalid LLVM IR"
          exit 1
        fi
        echo "opt-22 rejected the known-invalid LLVM IR fixture as required"

    - name: Test return15 example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/return15.aero -o ../../return15.ll
        opt-22 -passes=verify -disable-output ../../return15.ll
        llc-22 -verify-machineinstrs ../../return15.ll -o /dev/null
        llc-22 -filetype=obj ../../return15.ll -o ../../return15.o
        clang-22 ../../return15.o -o ../../return15
        set +e
        ../../return15
        exit_code=$?
        set -e
        if [ $exit_code -ne 15 ]; then
          echo "Expected exit code 15, got $exit_code"
          exit 1
        fi
        echo "return15 test passed with exit code $exit_code"

    - name: Test variables example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/variables.aero -o ../../variables.ll
        opt-22 -passes=verify -disable-output ../../variables.ll
        llc-22 -verify-machineinstrs ../../variables.ll -o /dev/null
        llc-22 -filetype=obj ../../variables.ll -o ../../variables.o
        clang-22 ../../variables.o -o ../../variables
        set +e
        ../../variables
        exit_code=$?
        set -e
        if [ $exit_code -ne 6 ]; then
          echo "Expected exit code 6, got $exit_code"
          exit 1
        fi
    - name: Test mixed arithmetic example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/mixed.aero -o ../../mixed.ll
        opt-22 -passes=verify -disable-output ../../mixed.ll
        llc-22 -verify-machineinstrs ../../mixed.ll -o /dev/null
        llc-22 -filetype=obj ../../mixed.ll -o ../../mixed.o
        clang-22 ../../mixed.o -o ../../mixed
        set +e
        ../../mixed
        exit_code=$?
        set -e
        if [ $exit_code -ne 7 ]; then
          echo "Expected exit code 7, got $exit_code"
          exit 1
        fi
        echo "mixed test passed with exit code $exit_code"

    - name: Test float operations example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/float_ops.aero -o ../../float_ops.ll
        opt-22 -passes=verify -disable-output ../../float_ops.ll
        llc-22 -verify-machineinstrs ../../float_ops.ll -o /dev/null
        llc-22 -filetype=obj ../../float_ops.ll -o ../../float_ops.o
        clang-22 ../../float_ops.o -o ../../float_ops
        set +e
        ../../float_ops
        exit_code=$?
        set -e
        if [ $exit_code -ne 7 ]; then
          echo "Expected exit code 7, got $exit_code"
          exit 1
        fi
        echo "float_ops test passed with exit code $exit_code"

    - name: Test typed empty numeric arrays example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/typed_empty_numeric_arrays.aero -o ../../typed_empty_numeric_arrays.ll
        opt-22 -passes=verify -disable-output ../../typed_empty_numeric_arrays.ll
        llc-22 -verify-machineinstrs ../../typed_empty_numeric_arrays.ll -o /dev/null
        llc-22 -filetype=obj ../../typed_empty_numeric_arrays.ll -o ../../typed_empty_numeric_arrays.o
        clang-22 ../../typed_empty_numeric_arrays.o -o ../../typed_empty_numeric_arrays
        set +e
        ../../typed_empty_numeric_arrays
        exit_code=$?
        set -e
        if [ $exit_code -ne 37 ]; then
          echo "Expected exit code 37, got $exit_code"
          exit 1
        fi
        echo "typed_empty_numeric_arrays test passed with exit code $exit_code"

    - name: Test fixed numeric array static length example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/fixed_numeric_array_len.aero -o ../../fixed_numeric_array_len.ll
        opt-22 -passes=verify -disable-output ../../fixed_numeric_array_len.ll
        llc-22 -verify-machineinstrs ../../fixed_numeric_array_len.ll -o /dev/null
        llc-22 -filetype=obj ../../fixed_numeric_array_len.ll -o ../../fixed_numeric_array_len.o
        clang-22 ../../fixed_numeric_array_len.o -o ../../fixed_numeric_array_len
        set +e
        ../../fixed_numeric_array_len
        exit_code=$?
        set -e
        if [ $exit_code -ne 37 ]; then
          echo "Expected exit code 37, got $exit_code"
          exit 1
        fi
        echo "fixed_numeric_array_len test passed with exit code $exit_code"

    - name: Test static string character length example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/static_string_len.aero -o ../../static_string_len.ll
        opt-22 -passes=verify -disable-output ../../static_string_len.ll
        llc-22 -verify-machineinstrs ../../static_string_len.ll -o /dev/null
        llc-22 -filetype=obj ../../static_string_len.ll -o ../../static_string_len.o
        clang-22 ../../static_string_len.o -o ../../static_string_len
        set +e
        ../../static_string_len
        exit_code=$?
        set -e
        if [ $exit_code -ne 33 ]; then
          echo "Expected exit code 33, got $exit_code"
          exit 1
        fi
        echo "static_string_len test passed with exit code $exit_code"

    - name: Test static string equality example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/static_string_equality.aero -o ../../static_string_equality.ll
        opt-22 -passes=verify -disable-output ../../static_string_equality.ll
        llc-22 -verify-machineinstrs ../../static_string_equality.ll -o /dev/null
        llc-22 -filetype=obj ../../static_string_equality.ll -o ../../static_string_equality.o
        clang-22 ../../static_string_equality.o -o ../../static_string_equality
        set +e
        ../../static_string_equality
        exit_code=$?
        set -e
        if [ $exit_code -ne 41 ]; then
          echo "Expected exit code 41, got $exit_code"
          exit 1
        fi
        echo "static_string_equality test passed with exit code $exit_code"

    - name: Test static string Boolean predicates example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/static_string_predicates.aero -o ../../static_string_predicates.ll
        opt-22 -passes=verify -disable-output ../../static_string_predicates.ll
        llc-22 -verify-machineinstrs ../../static_string_predicates.ll -o /dev/null
        llc-22 -filetype=obj ../../static_string_predicates.ll -o ../../static_string_predicates.o
        clang-22 ../../static_string_predicates.o -o ../../static_string_predicates
        set +e
        ../../static_string_predicates
        exit_code=$?
        set -e
        if [ $exit_code -ne 43 ]; then
          echo "Expected exit code 43, got $exit_code"
          exit 1
        fi
        echo "static_string_predicates test passed with exit code $exit_code"

    - name: Test direct module integration example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/direct_module_integration/main.aero -o ../../direct_module_integration.ll
        opt-22 -passes=verify -disable-output ../../direct_module_integration.ll
        llc-22 -verify-machineinstrs ../../direct_module_integration.ll -o /dev/null
        llc-22 -filetype=obj ../../direct_module_integration.ll -o ../../direct_module_integration.o
        clang-22 ../../direct_module_integration.o -o ../../direct_module_integration
        set +e
        ../../direct_module_integration
        exit_code=$?
        set -e
        if [ $exit_code -ne 47 ]; then
          echo "Expected exit code 47, got $exit_code"
          exit 1
        fi
        echo "direct_module_integration test passed with exit code $exit_code"

    - name: Test monomorphic scalar struct integration example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/monomorphic_scalar_structs.aero -o ../../monomorphic_scalar_structs.ll
        opt-22 -passes=verify -disable-output ../../monomorphic_scalar_structs.ll
        llc-22 -verify-machineinstrs ../../monomorphic_scalar_structs.ll -o /dev/null
        llc-22 -filetype=obj ../../monomorphic_scalar_structs.ll -o ../../monomorphic_scalar_structs.o
        clang-22 ../../monomorphic_scalar_structs.o -o ../../monomorphic_scalar_structs
        set +e
        ../../monomorphic_scalar_structs
        exit_code=$?
        set -e
        if [ $exit_code -ne 53 ]; then
          echo "Expected exit code 53, got $exit_code"
          exit 1
        fi
        echo "struct example passed with exit code 53"

    - name: Test scalar struct Copy transport integration example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/scalar_struct_copy_transport.aero -o ../../scalar_struct_copy_transport.ll
        opt-22 -passes=verify -disable-output ../../scalar_struct_copy_transport.ll
        llc-22 -verify-machineinstrs ../../scalar_struct_copy_transport.ll -o /dev/null
        llc-22 -filetype=obj ../../scalar_struct_copy_transport.ll -o ../../scalar_struct_copy_transport.o
        clang-22 ../../scalar_struct_copy_transport.o -o ../../scalar_struct_copy_transport
        set +e
        ../../scalar_struct_copy_transport
        exit_code=$?
        set -e
        if [ $exit_code -ne 63 ]; then
          echo "Expected exit code 63, got $exit_code"
          exit 1
        fi
        echo "struct Copy transport example passed with exit code 63"

    - name: Test fixed Copy-struct array integration example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/fixed_copy_struct_arrays/main.aero -o ../../fixed_copy_struct_arrays.ll
        opt-22 -passes=verify -disable-output ../../fixed_copy_struct_arrays.ll
        llc-22 -verify-machineinstrs ../../fixed_copy_struct_arrays.ll -o /dev/null
        llc-22 -filetype=obj ../../fixed_copy_struct_arrays.ll -o ../../fixed_copy_struct_arrays.o
        clang-22 ../../fixed_copy_struct_arrays.o -o ../../fixed_copy_struct_arrays
        set +e
        ../../fixed_copy_struct_arrays
        exit_code=$?
        set -e
        if [ $exit_code -ne 77 ]; then
          echo "Expected exit code 77, got $exit_code"
          exit 1
        fi
        echo "fixed Copy-struct array example passed with exit code 77"

    - name: Test fixed Copy-array transport integration example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/fixed_copy_array_transport/main.aero -o ../../fixed_copy_array_transport.ll
        opt-22 -passes=verify -disable-output ../../fixed_copy_array_transport.ll
        llc-22 -verify-machineinstrs ../../fixed_copy_array_transport.ll -o /dev/null
        llc-22 -filetype=obj ../../fixed_copy_array_transport.ll -o ../../fixed_copy_array_transport.o
        clang-22 ../../fixed_copy_array_transport.o -o ../../fixed_copy_array_transport
        set +e
        ../../fixed_copy_array_transport
        exit_code=$?
        set -e
        if [ $exit_code -ne 91 ]; then
          echo "Expected exit code 91, got $exit_code"
          exit 1
        fi
        echo "fixed Copy-array transport example passed with exit code 91"

    - name: Test acyclic Copy-aggregate integration example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/acyclic_copy_aggregates/main.aero -o ../../acyclic_copy_aggregates.ll
        opt-22 -passes=verify -disable-output ../../acyclic_copy_aggregates.ll
        llc-22 -verify-machineinstrs ../../acyclic_copy_aggregates.ll -o /dev/null
        llc-22 -filetype=obj ../../acyclic_copy_aggregates.ll -o ../../acyclic_copy_aggregates.o
        clang-22 ../../acyclic_copy_aggregates.o -o ../../acyclic_copy_aggregates
        set +e
        ../../acyclic_copy_aggregates
        exit_code=$?
        set -e
        if [ $exit_code -ne 107 ]; then
          echo "Expected exit code 107, got $exit_code"
          exit 1
        fi
        echo "acyclic Copy-aggregate example passed with exit code 107"

    - name: Test local scalar-reference integration example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/local_scalar_references/main.aero -o ../../local_scalar_references.ll
        opt-22 -passes=verify -disable-output ../../local_scalar_references.ll
        llc-22 -verify-machineinstrs ../../local_scalar_references.ll -o /dev/null
        llc-22 -filetype=obj ../../local_scalar_references.ll -o ../../local_scalar_references.o
        clang-22 ../../local_scalar_references.o -o ../../local_scalar_references
        set +e
        ../../local_scalar_references
        exit_code=$?
        set -e
        if [ $exit_code -ne 127 ]; then
          echo "Expected exit code 127, got $exit_code"
          exit 1
        fi
        echo "local scalar-reference example passed with exit code 127"

    - name: Test owned unit-enum match integration example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/unit_enum_matches/main.aero -o ../../unit_enum_matches.ll
        opt-22 -passes=verify -disable-output ../../unit_enum_matches.ll
        llc-22 -verify-machineinstrs ../../unit_enum_matches.ll -o /dev/null
        llc-22 -filetype=obj ../../unit_enum_matches.ll -o ../../unit_enum_matches.o
        clang-22 ../../unit_enum_matches.o -o ../../unit_enum_matches
        set +e
        ../../unit_enum_matches
        exit_code=$?
        set -e
        if [ $exit_code -ne 149 ]; then
          echo "Expected exit code 149, got $exit_code"
          exit 1
        fi
        echo "owned unit-enum match example passed with exit code 149"

    - name: Test owned unit-enum transport integration example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/unit_enum_transport/main.aero -o ../../unit_enum_transport.ll
        opt-22 -passes=verify -disable-output ../../unit_enum_transport.ll
        llc-22 -verify-machineinstrs ../../unit_enum_transport.ll -o /dev/null
        llc-22 -filetype=obj ../../unit_enum_transport.ll -o ../../unit_enum_transport.o
        clang-22 ../../unit_enum_transport.o -o ../../unit_enum_transport
        set +e
        ../../unit_enum_transport
        exit_code=$?
        set -e
        if [ $exit_code -ne 173 ]; then
          echo "Expected exit code 173, got $exit_code"
          exit 1
        fi
        echo "owned unit-enum transport example passed with exit code 173"

    - name: Test owned scalar-payload enum integration example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/scalar_payload_enum/main.aero -o ../../scalar_payload_enum.ll
        opt-22 -passes=verify -disable-output ../../scalar_payload_enum.ll
        llc-22 -verify-machineinstrs ../../scalar_payload_enum.ll -o /dev/null
        llc-22 -filetype=obj ../../scalar_payload_enum.ll -o ../../scalar_payload_enum.o
        clang-22 ../../scalar_payload_enum.o -o ../../scalar_payload_enum
        set +e
        ../../scalar_payload_enum
        exit_code=$?
        set -e
        if [ $exit_code -ne 181 ]; then
          echo "Expected exit code 181, got $exit_code"
          exit 1
        fi
        echo "owned scalar-payload enum example passed with exit code 181"

    - name: Test owned scalar-payload enum transport integration example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/payload_enum_transport/main.aero -o ../../payload_enum_transport.ll
        opt-22 -passes=verify -disable-output ../../payload_enum_transport.ll
        llc-22 -verify-machineinstrs ../../payload_enum_transport.ll -o /dev/null
        llc-22 -filetype=obj ../../payload_enum_transport.ll -o ../../payload_enum_transport.o
        clang-22 ../../payload_enum_transport.o -o ../../payload_enum_transport
        set +e
        ../../payload_enum_transport
        exit_code=$?
        set -e
        if [ $exit_code -ne 197 ]; then
          echo "Expected exit code 197, got $exit_code"
          exit 1
        fi
        echo "owned scalar-payload enum transport example passed with exit code 197"

    - name: Test reference-parameter transport integration example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/reference_parameter_transport/main.aero -o ../../reference_parameter_transport.ll
        opt-22 -passes=verify -disable-output ../../reference_parameter_transport.ll
        llc-22 -verify-machineinstrs ../../reference_parameter_transport.ll -o /dev/null
        llc-22 -filetype=obj ../../reference_parameter_transport.ll -o ../../reference_parameter_transport.o
        clang-22 ../../reference_parameter_transport.o -o ../../reference_parameter_transport
        set +e
        ../../reference_parameter_transport
        exit_code=$?
        set -e
        if [ $exit_code -ne 211 ]; then
          echo "Expected exit code 211, got $exit_code"
          exit 1
        fi
        echo "reference-parameter transport example passed with exit code 211"

    - name: Test mutable scalar reassignment integration example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/scalar_reassignment/main.aero -o ../../scalar_reassignment.ll
        opt-22 -passes=verify -disable-output ../../scalar_reassignment.ll
        llc-22 -verify-machineinstrs ../../scalar_reassignment.ll -o /dev/null
        llc-22 -filetype=obj ../../scalar_reassignment.ll -o ../../scalar_reassignment.o
        clang-22 ../../scalar_reassignment.o -o ../../scalar_reassignment
        set +e
        ../../scalar_reassignment
        exit_code=$?
        set -e
        if [ $exit_code -ne 227 ]; then
          echo "Expected exit code 227, got $exit_code"
          exit 1
        fi
        echo "mutable scalar reassignment example passed with exit code 227"

    - name: Test local mutable scalar-reference integration example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/mutable_scalar_references/main.aero -o ../../mutable_scalar_references.ll
        opt-22 -passes=verify -disable-output ../../mutable_scalar_references.ll
        llc-22 -verify-machineinstrs ../../mutable_scalar_references.ll -o /dev/null
        llc-22 -filetype=obj ../../mutable_scalar_references.ll -o ../../mutable_scalar_references.o
        clang-22 ../../mutable_scalar_references.o -o ../../mutable_scalar_references
        set +e
        ../../mutable_scalar_references
        exit_code=$?
        set -e
        if [ $exit_code -ne 239 ]; then
          echo "Expected exit code 239, got $exit_code"
          exit 1
        fi
        echo "local mutable scalar-reference example passed with exit code 239"

    - name: Test mutable scalar-reference parameter transport integration example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/mutable_reference_parameter_transport/main.aero -o ../../mutable_reference_parameter_transport.ll
        opt-22 -passes=verify -disable-output ../../mutable_reference_parameter_transport.ll
        llc-22 -verify-machineinstrs ../../mutable_reference_parameter_transport.ll -o /dev/null
        llc-22 -filetype=obj ../../mutable_reference_parameter_transport.ll -o ../../mutable_reference_parameter_transport.o
        clang-22 ../../mutable_reference_parameter_transport.o -o ../../mutable_reference_parameter_transport
        set +e
        ../../mutable_reference_parameter_transport
        exit_code=$?
        set -e
        if [ $exit_code -ne 251 ]; then
          echo "Expected exit code 251, got $exit_code"
          exit 1
        fi
        echo "mutable scalar-reference parameter transport example passed with exit code 251"

    - name: Test mutable scalar-reference identifier reborrow integration example
      run: |
        cd src/compiler
        cargo run -- build ../../examples/mutable_reference_reborrowing/main.aero -o ../../mutable_reference_reborrowing.ll
        opt-22 -passes=verify -disable-output ../../mutable_reference_reborrowing.ll
        llc-22 -verify-machineinstrs ../../mutable_reference_reborrowing.ll -o /dev/null
        llc-22 -filetype=obj ../../mutable_reference_reborrowing.ll -o ../../mutable_reference_reborrowing.o
        clang-22 ../../mutable_reference_reborrowing.o -o ../../mutable_reference_reborrowing
        set +e
        ../../mutable_reference_reborrowing
        exit_code=$?
        set -e
        if [ $exit_code -ne 253 ]; then
          echo "Expected exit code 253, got $exit_code"
          exit 1
        fi
        echo "mutable scalar-reference identifier reborrow example passed with exit code 253"

    - name: Test flat Copy-scalar tuple integration example
      shell: bash
      run: |
        cd src/compiler
        cargo run -- build ../../examples/flat_copy_tuples/main.aero -o ../../flat_copy_tuples.ll
        opt-22 -passes=verify -disable-output ../../flat_copy_tuples.ll
        llc-22 -verify-machineinstrs ../../flat_copy_tuples.ll -o /dev/null
        llc-22 -filetype=obj ../../flat_copy_tuples.ll -o ../../flat_copy_tuples.o
        clang-22 ../../flat_copy_tuples.o -o ../../flat_copy_tuples
        set +e
        ../../flat_copy_tuples
        exit_code=$?
        set -e
        if [ $exit_code -ne 23 ]; then
          echo "flat Copy-scalar tuple example returned $exit_code, expected 23"
          exit 1
        fi
        echo "flat Copy-scalar tuple example passed with exit code 23"

    - name: Test immutable Copy-place reference integration example
      shell: bash
      run: |
        cd src/compiler
        cargo run -- build ../../examples/copy_place_references/main.aero -o ../../copy_place_references.ll
        opt-22 -passes=verify -disable-output ../../copy_place_references.ll
        llc-22 -verify-machineinstrs ../../copy_place_references.ll -o /dev/null
        llc-22 -filetype=obj ../../copy_place_references.ll -o ../../copy_place_references.o
        clang-22 ../../copy_place_references.o -o ../../copy_place_references
        set +e
        ../../copy_place_references
        exit_code=$?
        set -e
        if [ $exit_code -ne 37 ]; then
          echo "immutable Copy-place reference example returned $exit_code, expected 37"
          exit 1
        fi
        echo "immutable Copy-place reference example passed with exit code 37"

    - name: Test mutable Copy-place reference integration example
      shell: bash
      run: |
        cd src/compiler
        cargo run -- build ../../examples/mutable_copy_place_references/main.aero -o ../../mutable_copy_place_references.ll
        opt-22 -passes=verify -disable-output ../../mutable_copy_place_references.ll
        llc-22 -verify-machineinstrs ../../mutable_copy_place_references.ll -o /dev/null
        llc-22 -filetype=obj ../../mutable_copy_place_references.ll -o ../../mutable_copy_place_references.o
        clang-22 ../../mutable_copy_place_references.o -o ../../mutable_copy_place_references
        set +e
        ../../mutable_copy_place_references
        exit_code=$?
        set -e
        if [ $exit_code -ne 59 ]; then
          echo "mutable Copy-place reference example returned $exit_code, expected 59"
          exit 1
        fi
        echo "mutable Copy-place reference example passed with exit code 59"

    - name: Test Copy-place reassignment integration example
      shell: bash
      run: |
        cd src/compiler
        cargo run -- build ../../examples/copy_place_reassignment/main.aero -o ../../copy_place_reassignment.ll
        opt-22 -passes=verify -disable-output ../../copy_place_reassignment.ll
        llc-22 -verify-machineinstrs ../../copy_place_reassignment.ll -o /dev/null
        llc-22 -filetype=obj ../../copy_place_reassignment.ll -o ../../copy_place_reassignment.o
        clang-22 ../../copy_place_reassignment.o -o ../../copy_place_reassignment
        set +e
        ../../copy_place_reassignment
        exit_code=$?
        set -e
        if [ $exit_code -ne 83 ]; then
          echo "Copy-place reassignment example returned $exit_code, expected 83"
          exit 1
        fi
        echo "Copy-place reassignment example passed with exit code 83"

    - name: Test recursive Copy-aggregate integration example
      shell: bash
      run: |
        cd src/compiler
        cargo run -- build ../../examples/recursive_copy_aggregates/main.aero -o ../../recursive_copy_aggregates.ll
        opt-22 -passes=verify -disable-output ../../recursive_copy_aggregates.ll
        llc-22 -verify-machineinstrs ../../recursive_copy_aggregates.ll -o /dev/null
        llc-22 -filetype=obj ../../recursive_copy_aggregates.ll -o ../../recursive_copy_aggregates.o
        clang-22 ../../recursive_copy_aggregates.o -o ../../recursive_copy_aggregates
        set +e
        ../../recursive_copy_aggregates
        exit_code=$?
        set -e
        if [ $exit_code -ne 109 ]; then
          echo "recursive Copy-aggregate example returned $exit_code, expected 109"
          exit 1
        fi
        echo "recursive Copy-aggregate example passed with exit code 109"

    - name: Test recursive CopyData payload enum integration example
      shell: bash
      run: |
        cd src/compiler
        cargo run -- build ../../examples/recursive_copy_payload_enum/main.aero -o ../../recursive_copy_payload_enum.ll
        opt-22 -passes=verify -disable-output ../../recursive_copy_payload_enum.ll
        llc-22 -verify-machineinstrs ../../recursive_copy_payload_enum.ll -o /dev/null
        llc-22 -filetype=obj ../../recursive_copy_payload_enum.ll -o ../../recursive_copy_payload_enum.o
        clang-22 -no-pie ../../recursive_copy_payload_enum.o -o ../../recursive_copy_payload_enum
        set +e
        ../../recursive_copy_payload_enum
        exit_code=$?
        set -e
        if [ $exit_code -ne 113 ]; then
          echo "Expected exit code 113, got $exit_code"
          exit 1
        fi
        echo "recursive CopyData payload enum example passed with exit code 113"

    - name: Test owned enum reassignment integration example
      shell: bash
      run: |
        cd src/compiler
        cargo run -- build ../../examples/owned_enum_reassignment/main.aero -o ../../owned_enum_reassignment.ll
        opt-22 -passes=verify -disable-output ../../owned_enum_reassignment.ll
        llc-22 -verify-machineinstrs ../../owned_enum_reassignment.ll -o /dev/null
        llc-22 -filetype=obj ../../owned_enum_reassignment.ll -o ../../owned_enum_reassignment.o
        clang-22 -no-pie ../../owned_enum_reassignment.o -o ../../owned_enum_reassignment
        set +e
        ../../owned_enum_reassignment
        exit_code=$?
        set -e
        if [ $exit_code -ne 131 ]; then
          echo "Expected exit code 131, got $exit_code"
          exit 1
        fi
        echo "owned enum reassignment example passed with exit code 131"

    - name: Test conditional enum ownership integration example
      shell: bash
      run: |
        cd src/compiler
        cargo run -- build ../../examples/conditional_enum_ownership/main.aero -o ../../conditional_enum_ownership.ll
        opt-22 -passes=verify -disable-output ../../conditional_enum_ownership.ll
        llc-22 -verify-machineinstrs ../../conditional_enum_ownership.ll -o /dev/null
        llc-22 -filetype=obj ../../conditional_enum_ownership.ll -o ../../conditional_enum_ownership.o
        clang-22 -no-pie ../../conditional_enum_ownership.o -o ../../conditional_enum_ownership
        set +e
        ../../conditional_enum_ownership
        exit_code=$?
        set -e
        if [ $exit_code -ne 137 ]; then
          echo "Expected exit code 137, got $exit_code"
          exit 1
        fi
        echo "conditional enum ownership example passed with exit code 137"

    - name: Test loop-local enum ownership integration example
      shell: bash
      run: |
        cd src/compiler
        cargo run -- build ../../examples/loop_local_enum_ownership/main.aero -o ../../loop_local_enum_ownership.ll
        opt-22 -passes=verify -disable-output ../../loop_local_enum_ownership.ll
        llc-22 -verify-machineinstrs ../../loop_local_enum_ownership.ll -o /dev/null
        llc-22 -filetype=obj ../../loop_local_enum_ownership.ll -o ../../loop_local_enum_ownership.o
        clang-22 -no-pie ../../loop_local_enum_ownership.o -o ../../loop_local_enum_ownership
        set +e
        ../../loop_local_enum_ownership
        exit_code=$?
        set -e
        if [ $exit_code -ne 149 ]; then
          echo "Expected exit code 149, got $exit_code"
          exit 1
        fi
        echo "loop-local enum ownership example passed with exit code 149"

    - name: Test intrinsic method contract integration example
      shell: bash
      run: |
        cd src/compiler
        cargo run -- build ../../examples/intrinsic_method_contract/main.aero -o ../../intrinsic_method_contract.ll
        opt-22 -passes=verify -disable-output ../../intrinsic_method_contract.ll
        llc-22 -verify-machineinstrs ../../intrinsic_method_contract.ll -o /dev/null
        llc-22 -filetype=obj ../../intrinsic_method_contract.ll -o ../../intrinsic_method_contract.o
        clang-22 -no-pie ../../intrinsic_method_contract.o -o ../../intrinsic_method_contract
        set +e
        ../../intrinsic_method_contract
        exit_code=$?
        set -e
        if [ $exit_code -ne 167 ]; then
          echo "Expected exit code 167, got $exit_code"
          exit 1
        fi
        echo "intrinsic method contract example passed with exit code 167"

    - name: Test function call contract integration example
      shell: bash
      run: |
        cd src/compiler
        cargo run -- build ../../examples/function_call_contract/main.aero -o ../../function_call_contract.ll
        opt-22 -passes=verify -disable-output ../../function_call_contract.ll
        llc-22 -verify-machineinstrs ../../function_call_contract.ll -o /dev/null
        llc-22 -filetype=obj ../../function_call_contract.ll -o ../../function_call_contract.o
        clang-22 -no-pie ../../function_call_contract.o -o ../../function_call_contract
        set +e
        ../../function_call_contract
        exit_code=$?
        set -e
        if [ $exit_code -ne 181 ]; then
          echo "Expected exit code 181, got $exit_code"
          exit 1
        fi
        echo "function call contract example passed with exit code 181"

    - name: Run tests
      run: cargo test --verbose
      working-directory: src/compiler

    - name: Run Clippy (correctness)
      run: cargo clippy --all-targets --all-features -- -D clippy::correctness
      working-directory: src/compiler


